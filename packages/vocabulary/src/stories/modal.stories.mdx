import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import Cross from '!raw-loader!@creativecommons/fonts/dist/assets/svg/symbols/times.svg';

<Meta title="Patterns/Modal" />

# Modal

export const ModalContainer = () => {
  return `<div>
<div class="overlay app-modal">
  <div class="modal relative" aria-modal="true" role="dialog">
    <header
      class="modal-header padding-top-bigger padding-left-bigger padding-right-normal padding-bottom-small"
    >
      <h3>App Modal Title</h3>
      <button
      type="button"
      class="close-button has-color-gray is-size-6 is-size-4-touch"
      :aria-label="$t('browse-page.aria.close')"
      >
        <span class="icon has-color-gray is-size-4 is-size-6-touch">
        ${Cross}
        </span>
      </button>
    </header>
    <div class="meta-card">
      <hr class="margin-bottom-bigger">
      <div class="modal-content padding-left-bigger padding-right-bigger padding-bottom-bigger">
        <p>This is modal content </p>
        <p>When adding a modal, you have to handle it's 'close' event to close it from the parent component.</p>
      </div>
    </div>
  </div>
</div>`
}

<Preview mdxSource={ModalContainer()}>
  <Story name="Modal">{
    ModalContainer()
  }</Story>
</Preview>
