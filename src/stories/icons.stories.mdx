import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { figmaConfig } from './helpers'

<Meta title="Assets/Vocabulary Icons"/>

# Vocabulary Icons

Vocabulary Icons is an icon font developed for use in Vocabulary, a cohesive design system to unite the web-facing
Creative Commons.

The icon font selectively includes assets from [Font Awesome](https://fontawesome.com) based on the icons used
throughout the design library on Figma and bundles them using [IcoMoon](https://icomoon.io/) to get the icons in the
smallest possible file size bundle.


export const icons = (icon_name) => {
return `<svg>
<use href="icons/${icon_name}.svg#Layer_1"></use>
</svg>`
}


<Preview>
  <Story name="BY" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('by')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="CC" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('cc')}</div>
  `}
  </Story>
</Preview>


<Preview>
  <Story name="NC EU" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('nc-eu')}</div>
  `}
  </Story>
</Preview>


<Preview>
  <Story name="NC JP" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('nc-jp')}</div>
  `}
  </Story>
</Preview>


<Preview>
  <Story name="NC" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('nc')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="ND" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('nd')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="PD" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('pd')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="REMIX" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('remix')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="SA" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('sa')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="SAMPLING PLUS" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('sampling.plus')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="SAMPLING" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('sampling')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="SHARE" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('share')}</div>
  `}
  </Story>
</Preview>

<Preview>
  <Story name="ZERO" parameters={{
    design: figmaConfig("732%3A0")
  }}>{`
  <div>${icons('zero')}</div>
  `}
  </Story>
</Preview>


# Usage

The icons can be used in your apps by defining an SVG tag and specifying the href of icon to be used.

**Example:**

```html
<svg>
  <use
    href="@creativecommons/vocabulary/src/assets/icons/by.svg">
  </use>
</svg>
```
