import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Select field" />

# Select field

<Preview>
  <Story name="Default">{`
    <div class="select">
      <select>
        <option disabled>Select</option>
        <option>Option A</option>
        <option>Option B</option>
      </select>
    </div>
  `}</Story>
</Preview>

<Preview>
  <Story name="Disabled">{`
    <div class="select">
      <select disabled>
        <option disabled>Select</option>
        <option>Option A</option>
        <option>Option B</option>
      </select>
    </div>
  `}</Story>
</Preview>
