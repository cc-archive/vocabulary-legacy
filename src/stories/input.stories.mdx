import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { figmaConfig } from './helpers'

<Meta title="Form/Input" />

# Input

export const inputType = (inputClass) => `<input class="${inputClass}" type="text" placeholder="Placeholder text"/>
    <br/><br/>
    <input class="${inputClass}" type="text" value="Typed text"/>
    <br/><br/>
    <input class="${inputClass}" type="text" value="Fixed text" disabled/>`


<Preview mdxSource={inputType('input')}>
  <Story name="Normal" parameters={{
    design: figmaConfig('1%3A24')
  }}>{`
    ${inputType('input')}
  `}</Story>
</Preview>

<Preview mdxSource={inputType('input is-medium')}>
  <Story name="Big" parameters={{
    design: figmaConfig('1%3A24')
  }}>{`
    ${inputType('input is-medium')}
  `}</Story>
</Preview>

<Preview mdxSource={inputType('input is-large')}>
  <Story name="Huge" parameters={{
    design: figmaConfig('1%3A24')
  }}>{`
    ${inputType('input is-large')}
  `}</Story>
</Preview>
