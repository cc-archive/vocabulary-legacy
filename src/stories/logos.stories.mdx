import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { figmaConfig } from './helpers'

<Meta title="Assets/Logos" />

# Logos

## Main logo

export const svgCode = (viewBoxX, viewBoxY, fileName, id) => `<svg
  xmlns="http://www.w3.org/2000/svg"
  preserveAspectRatio="xMidYMid meet"
  viewBox="0 0 ${viewBoxX} ${viewBoxY}">
  <use href="logos/${fileName}.svg#${id}"></use>
</svg>`

export const blackText = (content) => `<div class="has-text-black">${content}</div>`

export const whiteText = (content) => `<div class="has-text-white">${content}</div>`

export const plainText = (content) =>`<div>${content}</div>`

export const logomark = `<style>svg { height: 73px; }</style>
${svgCode(304, 73, 'cc/logomark', 'logomark')}`

<Preview mdxSource={blackText(logomark)}>
  <Story name="CC Logomark" height="128px" parameters={{
    design: figmaConfig('1%3A13')
  }}>{
    blackText(logomark)
  }</Story>
</Preview>

<Preview mdxSource={whiteText(logomark)} style={{backgroundColor: 'black'}}>
  <Story name="CC Logomark Inverted" height="128px" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('476%3A0')
  }}>{
    whiteText(logomark)
  }</Story>
</Preview>

export const lettermark = `<style>svg { height: 72px; }</style>
${svgCode(72, 72, 'cc/lettermark', 'lettermark')}`

<Preview mdxSource={blackText(lettermark)}>
  <Story name="CC Lettermark" height="128px" parameters={{
    design: figmaConfig('712%3A8')
  }}>{
    blackText(lettermark)
  }</Story>
</Preview>

<Preview mdxSource={whiteText(lettermark)} style={{backgroundColor: 'black'}}>
  <Story name="CC Lettermark Inverted" height="128px" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('712%3A8')
  }}>{
    whiteText(lettermark)
  }</Story>
</Preview>

export const letterheart = `<style>svg { height: 72px; }</style>
${svgCode(80, 72, 'cc/letterheart', 'letterheart')}`

<Preview mdxSource={blackText(letterheart)}>
  <Story name="CC Letterheart" height="128px" parameters={{
    design: figmaConfig('712%3A7')
  }}>{
    blackText(letterheart)
  }</Story>
</Preview>

<Preview mdxSource={whiteText(letterheart)} style={{backgroundColor: 'black'}}>
  <Story name="CC Letterheart Inverted" height="128px" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('712%3A7')
  }}>{
    whiteText(letterheart)
  }</Story>
</Preview>

## Product logos

export const globalNetwork = `<style>svg { height: 79px; }</style>
${svgCode(280, 79, 'products/global_network', 'globalnetwork')}`

<Preview mdxSource={plainText(globalNetwork)}>
  <Story name="CC Global Network" height="128px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
    plainText(globalNetwork)
  }</Story>
</Preview>

export const openSource = `<style>svg { height: 76px; }</style>
${svgCode(250, 76, 'products/open_source', 'opensource')}`

<Preview mdxSource={plainText(openSource)}>
  <Story name="CC Open Source" height="128px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
    plainText(openSource)
  }</Story>
</Preview>

export const vocabulary = `<style>svg { height: 41px; }</style>
${svgCode(250, 41, 'products/vocabulary', 'vocabulary')}`

<Preview mdxSource={plainText(vocabulary)}>
  <Story name="CC Vocabulary" height="96px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
     plainText(vocabulary)
  }</Story>
</Preview>

export const search = `<style>svg { height: 81px; }</style>
${svgCode(250, 81, 'products/search', 'search')}`

<Preview mdxSource={plainText(search)}>
  <Story name="CC Search" height="128px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
    plainText(search)
  }</Story>
</Preview>

export const certificates = `<style>svg { height: 81px; }</style>
${svgCode(250, 81, 'products/certificates', 'certificates')}`

<Preview mdxSource={plainText(search)}>
  <Story name="CC Certificates" height="128px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
    plainText(certificates)
  }</Story>
</Preview>


export const chooser = `<style>svg { height: 76px; }</style>
${svgCode(250, 76, 'products/chooser', 'chooser')}`

<Preview mdxSource={plainText(chooser)}>
  <Story name="CC Chooser" height="128px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
    plainText(chooser)
  }</Story>
</Preview>

export const globalSummit = `<style>svg { height: 79px; }</style>
${svgCode(280, 79, 'products/global_summit', 'global_summit')}`

<Preview mdxSource={plainText(globalSummit)}>
  <Story name="CC Global Summit" height="128px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
    plainText(globalSummit)
  }</Story>
</Preview>

export const stateCommons = `<style>svg { height: 79px; }</style>
${svgCode(380, 79, 'products/state_of_the_commons', 'state_of_the_commons')}`

<Preview mdxSource={plainText(stateCommons)}>
  <Story name="CC State of the commons" height="128px" parameters={{
    design: figmaConfig('784%3A1')
  }}>{
    plainText(stateCommons)
  }</Story>
</Preview>