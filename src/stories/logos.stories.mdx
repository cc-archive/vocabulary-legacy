import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { figmaConfig } from './helpers'

<Meta title="Assets/Logos" />

# Logos

## Logos

export const svgCode = (viewBoxX, viewBoxY, fileName, id) => `<svg
  xmlns="http://www.w3.org/2000/svg"
  preserveAspectRatio="xMidyMid meet"
  viewBox="0 0 ${viewBoxX} ${viewBoxY}">
  <use href="logos/${fileName}.svg#${id}"></use>
</svg>`

export const logomark = `<style>svg { height: 73px; }</style>
${svgCode(304, 73, 'cc/logomark', 'creativecommons')}`

<Preview>
  <Story name="CC Logomark" parameters={{
    design: figmaConfig('1%3A13')
  }}>{`
    <div class="has-text-black">${logomark}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Logomark Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('476%3A0')
  }}>{`
    <div class="has-text-white">${logomark}</div>
  `}</Story>
</Preview>

export const lettermark = `<style>svg { height: 72px; }</style>
${svgCode(72, 72, 'cc/lettermark', 'creativecommons')}`

<Preview>
  <Story name="CC Lettermark" parameters={{
    design: figmaConfig('712%3A8')
  }}>{`
    <div class="has-text-black">${lettermark}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Lettermark Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('712%3A8')
  }}>{`
    <div class="has-text-white">${lettermark}</div>
  `}</Story>
</Preview>

export const letterheart = `<style>svg { height: 72px; }</style>
${svgCode(80, 72, 'cc/letterheart', 'creativecommons')}`

<Preview>
  <Story name="CC Letterheart" parameters={{
    design: figmaConfig('712%3A7')
  }}>{`
    <div class="has-text-black">${letterheart}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Letterheart Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('712%3A7')
  }}>{`
    <div class="has-text-white">${letterheart}</div>
  `}</Story>
</Preview>

export const globalNetwork = `<style>svg { height: 79px; }</style>
${svgCode(250, 79, 'products/global_network', 'globalnetwork')}`

<Preview>
  <Story name="CC Global Network" parameters={{
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-black">${globalNetwork}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Global Network Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-white">${globalNetwork}</div>
  `}</Story>
</Preview>

export const openSource = `<style>svg { height: 76px; }</style>
${svgCode(250, 76, 'products/open_source', 'opensource')}`

<Preview>
  <Story name="CC Open Source" parameters={{
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-black">${openSource}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Open Source Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-white">${openSource}</div>
  `}</Story>
</Preview>

export const vocabulary = `<style>svg { height: 41px; }</style>
${svgCode(250, 41, 'products/vocabulary', 'vocabulary')}`

<Preview>
  <Story name="CC Vocabulary" parameters={{
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-black">${vocabulary}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Vocabulary Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-white">${vocabulary}</div>
  `}</Story>
</Preview>

export const search = `<style>svg { height: 81px; }</style>
${svgCode(250, 81, 'products/search', 'search')}`

<Preview>
  <Story name="CC Search" parameters={{
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-black">${search}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Search Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-white">${search}</div>
  `}</Story>
</Preview>

export const certificates = (type) => `<style>svg { height: 174px; }</style>
<svg
  xmlns="http://www.w3.org/2000/svg"
  preserveAspectRatio="xMidyMid meet"
  viewBox="0 0 160 174">
  <use href="logos/products/certificates.svg#certificates"></use>
  <use href="logos/products/certificates.svg#${type}_dots"></use>
</svg>`

export const certificatesColorful = certificates('colorful')

<Preview>
  <Story name="CC Certificates Colorful" parameters={{
    design: figmaConfig('784%3A1')
  }}>{`
    ${certificatesColorful}
  `}</Story>
</Preview>

export const certificatesMonochrome = certificates('monochrome')

<Preview>
  <Story name="CC Certificates Monochrome" parameters={{
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-black">${certificatesMonochrome}</div>
  `}</Story>
</Preview>

<Preview style={{backgroundColor: 'black'}}>
  <Story name="CC Certificates Monochrome Inverted" parameters={{
    backgrounds: [
      { name: 'black', value: 'black', default: true }
    ],
    design: figmaConfig('784%3A1')
  }}>{`
    <div class="has-text-white">${certificatesMonochrome}</div>
  `}</Story>
</Preview>
