export default {
  props: {
    /**
     * _the state to indicate the component in_
     *
     * ∈ {`'negative'`, `'positive'`}
     */
    indication: {
      type: String,
      default: ''
    }
  }
}
