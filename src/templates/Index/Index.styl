@import "~@/assets/tokens/tokens.styl"
@import "~@/styles/page.styl"

.vocab.index
  page()

  main
    min-height 100vh
    background radial-gradient($color-orange, $color-orange-dark)

  footer.vocab.footer
    .panel
      height 100%

      display flex
      flex-direction column
      justify-content space-between

      .vocab.locale
        margin-top $space-normal

  &.easter-egg
    main
      background $color-green-dark
